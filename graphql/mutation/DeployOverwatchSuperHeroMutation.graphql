mutation {
	DeployOverwatchSuperHeroMutation(input: { superHeroId: 9, gameModeId: 12, userName: "_viwe" }) {
		id
		combatMapId
		deployedAt
		gameMode {
			name
			screenshot
		}
		superHero {
			name
			portrait
		}
	}
}